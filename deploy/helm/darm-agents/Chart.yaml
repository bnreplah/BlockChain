apiVersion: v2
name: darm-agents
description: >
  Deploys tiers of DARM-ANN agents (knowledgeable / generalist / narrow /
  worker). Each agent joins a VPN (Tailscale) on start and registers its tier +
  capabilities to the model's agent registry.
type: application
version: 0.1.0
appVersion: "6.0"
keywords: [darm-ann, agents, tailscale, tiers]
